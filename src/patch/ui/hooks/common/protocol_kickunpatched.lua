if _mpPatch then
    function _mpPatch.hooks.protocol_kickunpached_installHooks()

    end

    function _mpPatch.hooks.protocol_kickunpached_onUpdate()

    end

    function _mpPatch.hooks.protocol_kickunpached_onJoin(i)

    end

    function _mpPatch.hooks.protocol_kickunpached_onDisconnect(i)

    end
end