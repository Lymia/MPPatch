<?xml version="1.0" encoding="utf-8"?>
<GameData>
    <Language_en_US>
        <!-- These text strings are displayed on the main menu next to the version when an error occurs. -->
        <Row Tag="TXT_KEY_MPPATCH_BINARY_NOT_PATCHED">
            <!-- Used when the MpPatch .dll/.so patch isn't installed, or is an unexpected version. -->
            <Text>MpPatch binary patch error</Text>
        </Row>
        <Row Tag="TXT_KEY_MPPATCH_UNKNOWN_FAILURE">
            <!-- Used when MpPatch isn't loaded, but the source of the failure could not be determined. -->
            <Text>MpPatch failed to load</Text>
        </Row>

        <!-- These strings are used in the multiplayer game creation menu -->
        <Row Tag="TXT_KEY_MPPATCH_CANNOT_BE_DISABLED">
            <!-- This is used in the tooltip for DLC that cannot be disabled as a mod depends on it. -->
            <Text>This DLC cannot be disabled because the following mods depend on it:</Text>
        </Row>

        <!-- These strings are used by the joining room -->
        <Row Tag="TXT_KEY_MPPATCH_MOD_MISSING">
            <!-- This is used when the user tries to join a room that uses mods they don't have installed. -->
            <Text>You cannot join this room because the following mods aren't installed:</Text>
        </Row>

        <!-- These strings are used by the main mods menu -->
        <Row Tag="TXT_KEY_MPPATCH_NO_SINGLEPLAYER_SUPPORT">
            <!-- This is used when a mod does not support singleplayer. -->
            <Text>The following mods do not support singleplayer:</Text>
        </Row>
        <Row Tag="TXT_KEY_MPPATCH_NO_MULTIPLAYER_SUPPORT">
            <!-- This is used when a mod does not support multiplayer. -->
            <Text>The following mods do not support multiplayer:</Text>
        </Row>
    </Language_en_US>
</GameData>